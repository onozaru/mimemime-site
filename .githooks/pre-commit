#!/bin/sh
# mimemime サイト監査フック（2026-08-02更新: 変更ファイルのみ監査するよう調整）
SITE_AUDIT="C:/Users/新井雅彦/OneDrive/ドキュメント/Claude/Projects/mimemime/admin/scripts/site_audit.py"

python "$SITE_AUDIT" . --changed
STATUS=$?

if [ $STATUS -ne 0 ]; then
  echo ""
  echo "* site_audit.py が問題を検出したため、コミットを中止しました。"
  echo "  上記の内容を確認・修正してから再度コミットしてください。"
  echo "  誤検知等でどうしても急ぐ場合は 'git commit --no-verify' でスキップできます。"
  exit 1
fi

exit 0
